# SAM 2 Experiments

## Initial Setup
- Chose the **SAM 2** model for initial testing.
- Conducted tests using **Nvidia RTX 2060 GPU**.
- Faced installation issues on Windows, leading to a decision to install on **WSL**.
- Explored **WSL, Python, and environment configurations**.
- Installed **SAM 2** using **Conda**, creating a dedicated environment with necessary dependencies.

## Image Segmentation Tests
- Tested a **SAM 2 image predictor model checkpoint** with suggested images to understand the process.
- Applied the same model checkpoint to **multiphase flow images** (`geom1_qc2_0001`).
- Performed an **interactive segmentation refinement process**:
  - Selected **10 points** and labeled them as part of the segmentation or background.
  - Utilized **LabelMe** ([GitHub](https://github.com/wkentaro/labelme)) for annotation.
  - Encountered installation issues on Windows, requiring specific **WSL configurations**.
  - Created a separate **Conda environment** for LabelMe.
  - Conducted **three iterations** of the refinement process, selecting from three generated masks per iteration.
  - Observed that **higher-scoring masks** captured general features (entire pipe and bubbles), whereas **lower-scoring masks** segmented specific bubbles but missed some.

## Video Segmentation Tests
- Tested the **SAM 2 video predictor** with the same model checkpoint using a suggested video.
- Initial propagation was slow:
  - **First run**: 4 hours to process half of a 6-second video (~200 frames).
  - **After reset**: Full 200-frame video processed in **4 minutes**.
- Selected a **16-second video** (`qc2_qd1_10x_formacao.avi`, 600MB) for multiphase flow image testing.
- Converted video into frames using **FFmpeg**:
  - Installed FFmpeg inside the **dedicated Python environment**.
  - Used command line to extract **493 frames** into a directory.

## Performance Observations
- **First-time runs** of video segmentation were extremely slow:
  - 500-frame video: **10 hours** (~60-70s per frame).
  - Subsequent runs of the same model: **<1 hour** (~3-4s per frame).
  - A smaller **6-second video** showed a similar trend:
    - First run: **~20s per frame**.
    - Subsequent runs: **~1s per frame**.
- Explanation for **subsequent speed improvements**:
  - **First-time execution** triggers PyTorch and CUDA to **compile and optimize GPU kernels**.
  - **Compilation caching** significantly speeds up future runs.

## Results and Insights
- **Video segmentation provided better results** compared to single image segmentation.
- **Bubbles were accurately tracked** across frames in the video segmentation process, but these results can be misleading because the video used and the frame images had better identifiable bubbles, with the bubbles being bigger and contrasting well with the background.
- Performance improvements observed with **subsequent runs** due to **model caching** and **GPU optimization**.



## 📌 Training SAM2

### 🔧 Environment Setup

- A new **Conda environment** was created specifically for SAM2 training.
- Necessary dependencies were installed for training compatibility.

### 🗂 Dataset Preparation

- The **MOSE dataset** (~20 GB of images) was selected for the initial training phase.
- The dataset included unwanted **metadata files ending in `._*`**, likely generated by macOS.  
  - These files caused errors during training and were deleted to resolve the issue.

### ⚙️ Configuration Adjustments

To run SAM2 training on a **machine with a single NVIDIA RTX 2060 8GB GPU**, several adjustments were required:

- Reduced GPU count:
  - From 8 GPUs → 1 GPU.
- Worker threads:
  - Initially set to 10 → changed to **8 workers**.
- Despite this, the training still failed to run:
  - A **script was created to reduce image size** to accommodate GPU limitations.
- Additional code-level configuration changes were necessary.

### ✅ Training Execution

- After making all necessary changes, the model **`sam2.1_hiera_base_plus.pt`** successfully started training.
- The model was trained for **2 epochs** as a first experiment.
- The retrained model was saved to a different path, specified in the configuration.

### 📊 Training Statistics

#### Epoch 0:

```json
{
  "Losses/train_all_loss": 5.433111148870933,
  "Losses/train_all_loss_mask": 0.06000992083447879,
  "Losses/train_all_loss_dice": 3.34929156734521,
  "Losses/train_all_loss_iou": 0.8832383877693539,
  "Losses/train_all_loss_class": 0.00038280269219983853,
  "Losses/train_all_core_loss": 5.433111148870933,
  "Trainer/where": 0.49983410749834106,
  "Trainer/epoch": 0,
  "Trainer/steps_train": 3014
}
```

#### Epoch 1:

```json
{
  "Losses/train_all_loss": 4.941373555210939,
  "Losses/train_all_loss_mask": 0.053238480442063994,
  "Losses/train_all_loss_dice": 3.0783527595520654,
  "Losses/train_all_loss_iou": 0.7982217768366636,
  "Losses/train_all_loss_class": 2.9433205870214882e-05,
  "Losses/train_all_core_loss": 4.941373555210939,
  "Trainer/where": 0.999834107498341,
  "Trainer/epoch": 1,
  "Trainer/steps_train": 6028
}
```

- Final adjustment: a configuration variable was updated to **correctly extract** and reference the newly trained model.

---

## 🧠 Annotation with Multiphase Dataset

### 🛠 Setup with Label Studio

- Installed and configured **Label Studio**.
- Followed the official guide from [HumanSignal's label-studio-ml-backend GitHub repo](https://github.com/HumanSignal/label-studio-ml-backend/tree/master).
- Pre-annotation using SAM2 was enabled using:
  - [Segment Anything 2 - Image](https://github.com/HumanSignal/label-studio-ml-backend/tree/master/label_studio_ml/examples/segment_anything_2_image)

### ⚙️ Infrastructure

- Two backend hosts were configured:
  - One for the **ML model inference**.
  - One for the **Label Studio interface**.

### 🐛 Issue with Pre-Annotation

- Problem: SAM2 model did not respond to inputs from the annotation interface.
- Solution: After extensive **debugging and testing**, the issue was resolved.

### 🖍 Annotation Process

- **Annotation workflow**:
  - Auto-Detect: ✅ Enabled
  - Auto-Annotation: ✅ Enabled
  - Auto-Accept Suggestion: ✅ Enabled
- **Annotation label type**:
  - Initially tested with **KeyPointLabel**.
  - Concluded that **RectangleLabel** produced more accurate predictions.
- Each **bubble** in every image was annotated.

---

## 🧾 Exporting Annotations

### ❌ Incompatible Formats

- Tried exporting in **YOLO and COCO** formats.
  - These resulted in **empty `.txt` files** due to format incompatibility with SAM2 expectations.

### ✅ SAM2-Compatible Export Strategy

- SAM2 requires:
  - One image (`.png`)
  - One **corresponding mask image** (`.png`) with the same filename.

### 📁 Issues and Solution

- Exporting with **Brush Labels to PNG**:
  - Provided **individual PNGs for each annotation**.
  - PNGs named using task ID, **not the original image name**.
- JSON file export **mapped task ID to the actual image name**.
  - Example: Label Studio file `49df7abe-00001.jpg` maps to image `00001.jpg`.

### 🧪 Developed Script: `convert.ipynb`

This script handles the process of combining masks and matching them with the original images:

#### Script Workflow:

1. Load exported JSON.
2. Parse each task for the **original image filename**.
3. Group all **mask PNGs (`task-<id>*`)** per image.
4. Merge masks into a single PNG (preserving label values).
5. Save merged mask as `Annotations_merged/<original_image>.png`.

> _(Insert example merged mask image here)_

### 📂 Resulting Structure:

```
Images/                  # Original source images
Annotations_merged/      # Combined masks with correct naming
```

---

## 🧬 Multiphase & Bubbly Flow Datasets

- **Multiple datasets** were annotated:
  - Some with **multiphase bubble images**
  - Others with **bubbly flow images**

> _(Insert image examples of both types)_

---

## 🎞 Video Frame Extraction

### 🔄 Converting Videos to Frames

- Initially attempted with a shell script `extract_frames.sh` using **ffmpeg**:
  - Faced issues with **recursive path traversal**.
- Switched to Python script: `extract_frames.py`:
  - Used **OpenCV (cv2)** library.
  - Extracted **1 frame per second** from `.avi` videos.
  - Maintained directory structure.

---

## 🧠 Advanced Label Studio Annotations for Object Tracking

- Focused on **multiple object tracking** (up to 5 bubbles per image).
- Used **color-coded masks** to differentiate objects.
- Based on how the **MOSE dataset** is structured:
  - Multiple bubbles per frame, each tracked across frames.
  - Required adapting the annotation process for SAM2's **object segmentation and tracking capabilities**.

### 🧪 Improved Script: `convert_colored.ipynb`

An enhanced version of `convert.ipynb`:

#### Key Features:

- Parses `annotation_id` from JSON.
- Groups masks by:
  - `task_id`
  - `annotation_id`
- Converts binary masks to **colored masks**:
  - Each annotation gets a **unique color**.
- Enables **object tracking** by label across multiple frames.

> _(Insert example of color-coded mask image here)_

### 📁 Resulting Directory Structure:

```
Images/video1/*.png                  # Frames from video
AnnotationColored/video1/*.png      # Color-coded masks per frame
```

- Each bubble gets a unique color (up to 5 different ones).
- Masks reflect consistent object identity across frames.

---

## 🎬 Annotation Summary

- Annotated:
  - **3 videos**
  - ~60 images total
  - Each image labeled with **3 to 5 unique bubbles**
  


## 🧪 SAM2 - Multiphase Experiment

## 📊 Experiment Report: Training SAM2 on a Minimal Dataset

---

## 🧪 Training Setup

| Parameter               | Value                        |
|-------------------------|------------------------------|
| Model                   | SAM2 (Hiera Base+)           |
| Pretrained Checkpoint   | `sam2.1_hiera_base_plus.pt`  |
| Input Resolution        | 256×256 (due to GPU limits)  |
| Train Batch Size        | 1                            |
| Frames per Sample       | 10                           |
| Max Objects per Sample  | 3                            |
| Base Learning Rate      | 5e-6                         |
| Vision Encoder LR       | 3e-6                         |
| Epochs                  | 10                           |
| Phases per Epoch        | 1                            |
| Data Loader Workers     | 8                            |
| Samples in Dataset      | 3 (very limited)             |

---

## 📋 Losses per Epoch

| Epoch | Total Loss | Mask Loss | Dice Loss | IoU Loss | Class Loss |
|-------|------------|-----------|-----------|----------|-------------|
| 0     | 0.6464     | 0.0062    | 0.1751    | 0.1920   | 0.1555      |
| 1     | 0.6137     | 0.0003    | 0.0241    | 0.0132   | 0.5706      |
| 2     | 0.1003     | 0.0004    | 0.0393    | 0.0328   | 0.0197      |
| 3     | 0.0031     | 0.0000    | 0.0000    | 0.0000   | 0.0031      |
| 4     | 6.2204     | 0.0725    | 2.5847    | 0.4475   | 1.7377      |
| 5     | 2.5428     | 0.0182    | 1.4507    | 0.7088   | 0.0201      |
| 6     | 2.2475     | 0.0063    | 0.8674    | 0.2058   | 1.0477      |
| 7     | 0.5301     | 0.0003    | 0.0231    | 0.0142   | 0.4870      |
| 8     | 4.8551     | 0.0271    | 2.3609    | 1.1307   | 0.8206      |
| 9     | 9.8234     | 0.3174    | 1.6086    | 0.7362   | 1.1308      |

---

## 🔍 Observations

### **Early Epochs (0–3)**

- Rapid convergence.
- Losses approach zero by Epoch 3, indicating **memorization** of the tiny dataset.
- Very low Dice and IoU loss suggest near-perfect segmentation — likely **overfitting**.

### **Later Epochs (4–9)**

- Significant instability, with **total loss peaking at 9.82** in Epoch 9.
- Large fluctuations in **dice, iou, and class losses**, indicating:
  - Potential overfitting.
  - High sensitivity to specific samples.

---

## 🧠 Interpretation

- With only **3 samples** and a **batch size of 1**, the model **quickly overfits**.
- The sharp spikes suggest that the **loss landscape is volatile**, likely due to:
  - Lack of dataset variation.
  - High gradient variance due to **batch size = 1**.
  - Absence of regularization or validation feedback during training.
- Losses such as **Dice and IoU spiking** are concerning for segmentation quality and consistency.

---

## 📌 Next Steps

- 📷 **Visualize predictions** at various epochs to correlate trends in loss with actual mask quality.
- 🔄 Use **data augmentation** to create synthetic diversity and improve generalization.
- 🧊 **Freeze vision layers** or reduce trainable parameters to prevent overfitting.
- ⏹ Introduce **validation data and early stopping** to avoid unstable training runs.

---

## 🔧 TODO

- 🔍 **Review Mask R-CNN configuration** → `mask_rcnn_X_101_32x8d_FPN_3x`

